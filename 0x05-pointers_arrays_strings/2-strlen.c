#include "main.h"

/**
 * _strlen - returns the length of str
 *
 * @s: input string
 *
 * Return: length of string
 */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
		continue;
	}
	return (count);

}
